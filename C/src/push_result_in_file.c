#include "../includes/C-Algo.h"

void combine_and_resultfile(s_algo *algo)
{
	int result_arr[algo->home_arr_len];
	//TODO: make nodes(done) out of threadinfo and then use the len to go
	// through the array to combine them and write it into a file
}
